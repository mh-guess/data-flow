# Pin prefect to work around broken 3.7.0 in managed work pool base image.
# 3.7.0 has internal import inconsistency: task_engine.py references
# _aget_default_persist_result which doesn't exist in results.py.
# TODO: Remove pin once Prefect fixes the base image.
prefect[aws]==3.6.29

# prefect.workers.base imports `importlib_metadata` (the backport, not stdlib
# importlib.metadata). The managed work pool base image stopped shipping it
# around 2026-05-25 and all 3 daily flows crashed on import. Pin it here so
# we don't depend on the base image including it.
importlib_metadata>=8.0.0

# Tiingo API integration
requests>=2.31.0

# Volatility table pipeline
pandas>=2.0.0
pyarrow>=14.0.0
ruamel.yaml>=0.18.0

# Hedge-map ETL: numpy OLS regression (corrcoef/cov/var) for beta/R² computation.
numpy>=1.26.0
