# Pin prefect to work around broken 3.7.0 in managed work pool base image.
# 3.7.0 has internal import inconsistency: task_engine.py references
# _aget_default_persist_result which doesn't exist in results.py.
# TODO: Remove pin once Prefect fixes the base image.
prefect[aws]==3.6.29

# Tiingo API integration
requests>=2.31.0

# Volatility table pipeline
pandas>=2.0.0
pyarrow>=14.0.0
ruamel.yaml>=0.18.0
